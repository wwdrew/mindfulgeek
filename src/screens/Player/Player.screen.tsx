import React from 'react';
import { Text } from 'react-native';

export type PlayerScreenParams = undefined;

const PlayerScreen = () => {
  return <Text>Player Screen</Text>;
};

export default PlayerScreen;
